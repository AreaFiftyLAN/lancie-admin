{
  "private": true,
  "devDependencies": {
 },
  "scripts": {
    "build": "node ./node_modules/gulp/bin/gulp.js",
    "ensure-images": "node ./node_modules/gulp/bin/gulp.js ensure-images-optimized",
    "serve": "yarn run ensure-images && node ./node_modules/polyserve/bin/polyserve -p 5101 -P h2 --proxy-path api --proxy-target http://localhost:9000/api/",
    "serve-build": "yarn run ensure-images && node ./node_modules/polyserve/bin/polyserve build/bundled/ -p 5101 -P h2 --proxy-path api --proxy-target http://localhost:9000/api/"
  },
  "engines": {
    "node": ">=5.0.0"
  }
}
